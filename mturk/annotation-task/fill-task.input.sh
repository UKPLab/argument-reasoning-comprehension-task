#!/usr/bin/env bash
# fills the task.input with pages

# 00 devel
# set prefix for this task
#prefix="00-devel-"
#echo "urls" > ${prefix}task.input
#for i in `ls -1 html/00-devel` ; do echo ${i} >> ${prefix}task.input; done

# 01 pilot
# set prefix for this task
#prefix="01-pilot-claims"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 02 pilot
# set prefix for this task
#prefix="02-pilot-claims"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 03 pilot
# set prefix for this task
#prefix="03-pilot-claims"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 04 pilot
# set prefix for this task
#prefix="04-pilot-claims"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 05 pilot
# set prefix for this task
#prefix="05-pilot-claims"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 20 pilot stance
#prefix="20-pilot-stance"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 20 pilot stance
#prefix="21-pilot-stance"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 21 stance-batch-0001-5000-task
#prefix="22-stance-batch-0001-5000"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done


# 30 pilot reasons
#prefix="30-pilot-reasons"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 31 pilot reasons
#prefix="31-pilot-reasons"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 31 pilot reasons
#prefix="32-reasons-batch-0001-5000-2883args"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done




# 40 pilot gist
#prefix="40-pilot-reason-gist"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 42-reasons-gist-batch-0001-5000-5119reasons
#prefix="42-reasons-gist-batch-0001-5000-5119reasons"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 50 pilot warrants
#prefix="50-pilot-warrants"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done


# 51 pilot warrants
#prefix="51-pilot-warrants"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done


# 52 pilot warrants
#prefix="52-pilot-warrants"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 53 pilot warrants
#prefix="53-pilot-warrants"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 54 pilot warrants
#prefix="54-pilot-warrants"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 55 batch
#prefix="55-warrants-batch-0001-5000-4294reasons-0001-0600"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 56 qualification
#prefix="56-warrants-qualification"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 60 reason disambiguation
#prefix="60-pilot-reason-disambiguation"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 61 reason disambiguation batch
#prefix="61-reason-disambiguation-batch-0001-5000-4294reasons-0060-4294"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 70 reason disambiguation batch
#prefix="70-alternative-warrants-batch-0001-5000-4235reasons-001-600"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 71 batch warrants
#prefix="71-alternative-warrants-batch-0001-5000-4235reasons-001-600"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 72-alternative-warrants-batch-0001-5000-4235reasons-600-1955
#prefix="72-alternative-warrants-batch-0001-5000-4235reasons-600-1955"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 80-aw-validation-pilot
#prefix="80-aw-validation-pilot"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 81-aw-validation-pilot
#prefix="81-001-600aw-validation-batch-0050-2390-reason-claim-pairs"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 82-aw-validation-pilot
#prefix="82-600-1955aw-validation-batch-5342-reason-claim-pairs"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 90 original warrant pilot
#prefix="90-original-warrant-pilot"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 91-original-warrant-batch-0001-5000-batch-0100-0600
#prefix="91-original-warrant-batch-0001-5000-batch-0100-0600"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 92-original-warrant-batch-0001-5000-batch-0600-2613
#prefix="92-original-warrant-batch-0001-5000-batch-0600-2613"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 95-validation-task-pilot
#prefix="95-validation-task-pilot"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

# 96-validation-task-batch-0001-5000-full-batch-2447
#prefix="96-validation-task-batch-0001-5000-full-batch-2447"
#echo "urls" > ${prefix}-task.input
#for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done

prefix="99-upper-bound"
echo "urls" > ${prefix}-task.input
for i in `ls -1 html/${prefix}` ; do echo ${i} >> ${prefix}-task.input; done


